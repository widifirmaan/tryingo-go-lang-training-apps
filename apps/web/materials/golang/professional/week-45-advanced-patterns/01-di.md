# Dependency Injection

Implement dependency injection in Go using Wire and manual approaches.

## Manual DI

```go
type Service struct {
    repo    Repository
    cache   Cache
    logger  *slog.Logger
}

func NewService(repo Repository, cache Cache, logger *slog.Logger) *Service {
    return &Service{repo: repo, cache: cache, logger: logger}
}

// Manual wiring
func buildService() *Service {
    db := database.NewPostgres("postgres://localhost:5432/db")
    cache := cache.NewRedis("localhost:6379")
    logger := slog.New(slog.NewJSONHandler(os.Stdout, nil))
    repo := repository.NewUserRepository(db)
    return NewService(repo, cache, logger)
}
```

## Wire DI

```go
//go:build wireinject
// +build wireinject

package main

import (
    "github.com/google/wire"
    "myapp/internal/cache"
    "myapp/internal/config"
    "myapp/internal/database"
    "myapp/internal/handler"
    "myapp/internal/repository"
    "myapp/internal/service"
)

func InitializeApp() (*App, error) {
    wire.Build(
        config.Load,
        database.NewPostgres,
        database.NewRedis,
        cache.NewRedisCache,
        repository.NewUserRepository,
        repository.NewOrderRepository,
        service.NewUserService,
        service.NewOrderService,
        handler.NewUserHandler,
        handler.NewOrderHandler,
        NewRouter,
        NewApp,
    )
    return nil, nil
}

// Generate with: go run github.com/google/wire/cmd/wire
```

## Provider Patterns

```go
// Value binding
var ConfigSet = wire.Value(&Config{
    Port: 8080,
    Host: "0.0.0.0",
})

// Interface binding
var UserSet = wire.NewSet(
    repository.NewUserRepository,
    wire.Bind(new(UserRepository), new(*PostgresUserRepository)),
)

// Cleanup function
func NewDB(dsn string) (*sql.DB, func(), error) {
    db, err := sql.Open("postgres", dsn)
    if err != nil {
        return nil, nil, err
    }
    cleanup := func() {
        db.Close()
    }
    return db, cleanup, nil
}
```

## Testing with DI

```go
func TestService(t *testing.T) {
    mockRepo := new(MockRepository)
    mockCache := new(MockCache)
    svc := NewService(mockRepo, mockCache, slog.Default())

    // Test without external dependencies
    result, err := svc.DoSomething(context.Background())
}
```

## Practice
1. Refactor a monolith to use Wire
2. Create mock providers for testing
3. Implement clean interfaces for DI
