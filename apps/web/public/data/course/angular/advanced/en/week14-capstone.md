# Capstone: Complete Angular Store — Enterprise 0→Expert

> **Kategori:** Angular | **Level:** Advanced | **Minggu 14:** Capstone

## Learning Objectives

- Combine `Router` + `Service` + `HttpClient` + `OnPush` + `lazy` + `TestBed` into a store with `products` + `cart` + `auth` + `Vercel` deploy — all from W1-W13

---

## Why This Matters (Non-IT)

Without a capstone, 13 separate weeks — no idea how they combine. With it, shop `Home` eager + `admin` lazy + `products` `HttpClient` + `cart` `Service` + `OnPush` fast + `TestBed` tested → job-ready enterprise store.

---

## Program: Angular Capstone Store (Capstone)

Required features:
- `products` CRUD `HttpClient` `get/post` + `Service` `CartStore` + `OnPush` `trackBy`
- `admin` `loadComponent` lazy + `CanActivate` `auth` + `TestBed` 1 test `shows name`
- `deploy` `Vercel` `vercel --prod` + `Lighthouse` 90+

Structure:
```
src/app/
  products/ (HttpClient + Service)
  cart/ (ComponentStore)
  admin/ (loadComponent lazy)
  card/ (OnPush)
```

**Capstone task:** Deploy `shop-angular.vercel.app` + 2-min add-to-cart → checkout video + `ng test` PASS + `Lighthouse` screenshot.

**Source:** All W1-W13 `angular.dev`.

---

## Key Concepts

### Capstone = Combine All
`Router` map + `Service` warehouse + `HttpClient` courier + `OnPush` fast + `lazy` load + `TestBed` test.

---

## Beginner Friendly Explanation

### Analogy: Finished Enterprise Shop

- **W1-W5 `component`/`form`** = bricks
- **W6-W10 `Router`/`HttpClient`/`Store`** = map & warehouse
- **W11-W13 `TestBed`/`OnPush`/`lazy`** = test & fast
- **W14 Capstone = finished shop** — open a `Vercel` branch.

### Step 0 — Prepare Device

`ng new shop-angular --style=css --routing` + `ng serve` on `4200` + `ng test` + `vercel --prod` (done in W1, W11).

### How the Computer Reads It
1. `ng test` PASS + `Lighthouse` 90+ → enterprise-grade proof.
2. Public URL + video → portfolio done.

### 3 Must-Know Terms

1. **Capstone**: combine all
2. **Deploy**: open online branch
3. **Lighthouse**: shop score

---

## Experiments

- **Green:** All checklist items work on the live URL?
- **Yellow:** `ng test` fails → fix before deploy?
- **Red:** Skip `Lighthouse` → hidden perf debt? Audit.

---

## Challenge

**Complete Deployed Angular Shop:** `products` CRUD `HttpClient` + `cart` `Service` + `admin` lazy + `OnPush` + `TestBed` 1 test `shows name` + `Vercel` deploy + `Lighthouse` 90+ screenshot.

Criteria: `ng serve` + `ng test` PASS + `https://shop-angular.vercel.app` live + video.

---

## Mini Glossary

- **Capstone/deploy/Lighthouse**: combine/open-branch/score

---

## Summary

Week 14 of 14: **Angular Capstone** — complete enterprise store, **Angular 0→Expert DONE!** 🎉
