import { BaseGenerator } from './lib/base-generator.mjs';

// ─────────────────────────────────────────────────────────────────────────────
// RUBY ON RAILS CURRICULUM — pure research, zero framework influence
// Sources: Rails Official Guide, Rails Tutorial (Michael Hartl), GoRails,
//          Drifting Ruby, RailsCasts, Odin Project, Ruby on Rails API
// ─────────────────────────────────────────────────────────────────────────────
// Research consensus: 3 levels, 12 weeks total
//   Beginner (4w): setup → MVC → migrations → views
//   Intermediate (4w): auth → associations → testing → APIs
//   Advanced (4w): performance → background jobs → deployment → project
// Total: 12 weeks
// ─────────────────────────────────────────────────────────────────────────────

const gen = new BaseGenerator('rails', 'Ruby on Rails');

const LEVELS = [
  {
    levelId: 'beginer',
    nameId: 'Pemula',
    nameEn: 'Beginner',
    descId: 'Fundamental Rails: setup, MVC, migrations, views, dan scaffolding.',
    descEn: 'Rails fundamentals: setup, MVC, migrations, views, and scaffolding.',
  },
  {
    levelId: 'intermediate',
    nameId: 'Menengah',
    nameEn: 'Intermediate',
    descId: 'Rails menengah: authentication, associations, testing, dan API.',
    descEn: 'Intermediate Rails: authentication, associations, testing, and APIs.',
  },
  {
    levelId: 'advanced',
    nameId: 'Lanjutan',
    nameEn: 'Advanced',
    descId: 'Rails lanjutan: performance, background jobs, deployment, dan capstone.',
    descEn: 'Advanced Rails: performance, background jobs, deployment, and capstone.',
  },
];

const MODULES = [
  // ── BEGINNER (weeks 1-4) ──────────────────────────────────────────────────
  {
    week: 1, level: 'beginer', topicId: 'setup-rails',
    titleId: 'Setup & Instalasi Rails', titleEn: 'Setup & Rails Installation',
    programId: 'Project Pertama', programEn: 'First Project',
    levelNameId: 'Pemula', levelNameEn: 'Beginner',
    language: 'ruby',
    code: [
      '#!/usr/bin/env ruby',
      '# Ruby simulation',
      'puts "=== Ruby on Rails Setup ==="',
      'puts "gem install rails"',
      'puts "rails new my_app"',
      'puts "cd my_app"',
      'puts "rails server"',
      'puts "Server running on http://localhost:3000"',
      'puts ""',
      'puts "=== Rails Directory Structure ==="',
      'dirs = [',
      '    "app/",',
      '    "  controllers/",',
      '    "  models/",',
      '    "  views/",',
      '    "  helpers/",',
      '    "  assets/",',
      '    "config/",',
      '    "  routes.rb",',
      '    "  database.yml",',
      '    "db/",',
      '    "  migrate/",',
      '    "  seeds.rb",',
      '    "test/",',
      '    "Gemfile",',
      ']',
      'dirs.each { |d| puts "  #{d}" }',
      'puts ""',
      'puts "=== Key Commands ==="',
      'puts "rails new name      — Create new project"',
      'puts "rails server        — Start dev server (bin/rails s)"',
      'puts "rails console       — Interactive console (bin/rails c)"',
      'puts "rails generate      — Generate code (bin/rails g)"',
      'puts "rails db:migrate    — Run migrations"',
      'puts "rails routes        — List all routes"',
      'puts ""',
      'puts "=== Gemfile ==="',
      'puts "gem \'rails\', \'~> 7.0\'"',
      'puts "gem \'sqlite3\'         # Database"',
      'puts "gem \'puma\'            # Server"',
      'puts "gem \'devise\'          # Auth (later)"',
      'puts "bundle install"',
      '',
    ].join('\n'),
    objectivesId: [
      'Install Ruby dan Rails (Rails Guides: Getting Started)',
      'Memahami struktur folder Rails: app, config, db, test',
      'Rails CLI: server, console, generate, db:migrate',
      'Gemfile: dependency management dengan Bundler',
      'Convention over Configuration: filosofi Rails',
    ],
    objectivesEn: [
      'Install Ruby and Rails (Rails Guides: Getting Started)',
      'Understand Rails folder structure: app, config, db, test',
      'Rails CLI: server, console, generate, db:migrate',
      'Gemfile: dependency management with Bundler',
      'Convention over Configuration: Rails philosophy',
    ],
    explanationId: '### Instalasi Rails\n`gem install rails`, lalu `rails new nama_project`.\n\n### Struktur Folder\n- `app/` - MVC (controllers, models, views)\n- `config/` - routes, database, environment\n- `db/` - migrations, seeds\n- `test/` - test files\n\n### CLI\n`rails server` (bin/rails s), `rails console` (bin/rails c), `rails generate`.\n\n### Gemfile\nDefine dependencies. `bundle install` untuk install.\n\n### Convention over Configuration\nRails mengikuti konvensi: model `Post` -> tabel `posts` -> controller `PostsController`.',
    explanationEn: '### Rails Installation\n`gem install rails`, then `rails new name`.\n\n### Folder Structure\n- `app/` - MVC\n- `config/` - routes, database\n- `db/` - migrations, seeds\n- `test/` - test files\n\n### CLI\n`rails server`, `rails console`, `rails generate`.\n\n### Gemfile\nDefines dependencies. `bundle install` to install.\n\n### Convention over Configuration\nRails follows conventions: model `Post` -> table `posts` -> controller `PostsController`.',
    experimentsId: [
      'Install Rails dan buat project baru',
      'Jelajahi folder app/ dan lihat isinya',
      'Coba rails console untuk eksplorasi',
      'Buat route sederhana di routes.rb',
      'Pindah ke config/ dan lihat konfigurasi',
    ],
    experimentsEn: [
      'Install Rails and create new project',
      'Explore app/ folder and its contents',
      'Try rails console for exploration',
      'Create simple route in routes.rb',
      'Navigate config/ and view configuration',
    ],
    challengeId: 'Buat project Rails baru dengan 3 routes: home (/), about (/about), contact (/contact). Tampilkan teks berbeda di setiap route.',
    challengeEn: 'Create a new Rails project with 3 routes: home (/), about (/about), contact (/contact). Display different text on each route.',
    summaryId: 'Minggu 1 dari 12: **Setup & Instalasi Rails** (Level: Pemula). Fondasi Rails dimulai. Minggu depan: **MVC Architecture**.',
    summaryEn: 'Week 1 of 12: **Setup & Rails Installation** (Level: Beginner). Rails foundation begins. Next week: **MVC Architecture**.',
  },
  {
    week: 2, level: 'beginer', topicId: 'mvc-architecture',
    titleId: 'MVC Architecture', titleEn: 'MVC Architecture',
    programId: 'MVC in Action', programEn: 'MVC in Action',
    levelNameId: 'Pemula', levelNameEn: 'Beginner',
    language: 'ruby',
    code: [
      '#!/usr/bin/env ruby',
      'puts "=== Rails MVC Architecture ==="',
      'puts ""',
      'puts "=== Routes (config/routes.rb) ==="',
      'puts "Rails.application.routes.draw do"',
      'puts "  root \'posts#index\'"',
      'puts "  resources :posts"',
      'puts "end"',
      'puts ""',
      'puts "=== Controller (app/controllers/posts_controller.rb) ==="',
      'class PostsController',
      '  def index',
      '    @posts = Post.all',
      '    puts "  Action: index, Found #{@posts.length} posts"',
      '  end',
      '  def show',
      '    @post = Post.find(1)',
      '    puts "  Action: show, Post: #{@post[:title]}"',
      '  end',
      '  def new',
      '    @post = Post.new',
      '    puts "  Action: new, Form ready"',
      '  end',
      '  def create',
      '    @post = Post.new({title: "New Post", body: "Content"})',
      '    puts "  Action: create, Title: #{@post[:title]}"',
      '  end',
      'end',
      'puts "=== Model (app/models/post.rb) ==="',
      'class Post',
      '  attr_accessor :title, :body',
      '  def initialize(attrs = {})',
      '    @title = attrs[:title]',
      '    @body = attrs[:body]',
      '  end',
      '  def self.all',
      '    [',
      '      {id: 1, title: "First Post", body: "Hello Rails"},',
      '      {id: 2, title: "Second Post", body: "MVC is great"},',
      '    ]',
      '  end',
      '  def self.find(id)',
      '    {id: id, title: "Post ##{id}", body: "Content"}',
      '  end',
      'end',
      'puts "=== MVC Flow Simulation ==="',
      'puts "1. Request: GET /posts"',
      'puts "2. Router: routes to PostsController#index"',
      'puts "3. Controller: fetches Post.all from Model"',
      'puts "4. Model: returns data to Controller"',
      'puts "5. Controller: passes @posts to View"',
      'puts "6. View: renders HTML with posts"',
      'puts ""',
      'controller = PostsController.new',
      'controller.index',
      'controller.show',
      'controller.new',
      'controller.create',
      '',
    ].join('\n'),
    objectivesId: [
      'MVC pattern: Model (data), View (UI), Controller (logic)',
      'Routes: resources :posts untuk RESTful routing',
      'Controller: action methods (index, show, new, create)',
      'Model: ActiveRecord pattern, query methods',
      'View: ERB templates dengan instance variables',
    ],
    objectivesEn: [
      'MVC pattern: Model (data), View (UI), Controller (logic)',
      'Routes: resources :posts for RESTful routing',
      'Controller: action methods (index, show, new, create)',
      'Model: ActiveRecord pattern, query methods',
      'View: ERB templates with instance variables',
    ],
    explanationId: '### MVC Pattern\n- Model: data & business logic\n- View: presentation layer (ERB)\n- Controller: handle request, coordinate M & V\n\n### Routes\n`resources :posts` generate 7 RESTful routes.\n\n### Controller\nActions: `index`, `show`, `new`, `create`, `edit`, `update`, `destroy`.\n\n### Model\nActiveRecord: `Post.all`, `Post.find(id)`, `Post.where(...)`.\n\n### View\n`<%= @post.title %>` - ERB tags. Instance variables dari controller.',
    explanationEn: '### MVC Pattern\n- Model: data & business logic\n- View: presentation layer (ERB)\n- Controller: handle request, coordinate M & V\n\n### Routes\n`resources :posts` generates 7 RESTful routes.\n\n### Controller\nActions: `index`, `show`, `new`, `create`, `edit`, `update`, `destroy`.\n\n### Model\nActiveRecord: `Post.all`, `Post.find(id)`, `Post.where(...)`.\n\n### View\n`<%= @post.title %>` - ERB tags. Instance variables from controller.',
    experimentsId: [
      'Buat controller dengan scaffold',
      'Jelajari routes dengan rails routes',
      'Buat custom action di controller',
      'Coba params di controller',
      'Buat view dengan instance variables',
    ],
    experimentsEn: [
      'Create controller with scaffold',
      'Study routes with rails routes',
      'Create custom action in controller',
      'Try params in controller',
      'Create view with instance variables',
    ],
    challengeId: 'Buat CRUD posts: scaffold atau manual. Routes, controller actions, model, views. Deploy ke local.',
    challengeEn: 'Create CRUD posts: scaffold or manual. Routes, controller actions, model, views. Deploy to local.',
    summaryId: 'Minggu 2 dari 12: **MVC Architecture** (Level: Pemula). Heart of Rails. Minggu depan: **Migrations & Database**.',
    summaryEn: 'Week 2 of 12: **MVC Architecture** (Level: Beginner). Heart of Rails. Next week: **Migrations & Database**.',
  },
  {
    week: 3, level: 'beginer', topicId: 'migrations-database',
    titleId: 'Migrations & Database', titleEn: 'Migrations & Database',
    programId: 'Database Schema', programEn: 'Database Schema',
    levelNameId: 'Pemula', levelNameEn: 'Beginner',
    language: 'ruby',
    code: [
      '#!/usr/bin/env ruby',
      'puts "=== Rails Migrations ==="',
      'puts ""',
      'puts "=== Generate Migration ==="',
      'puts "rails generate migration CreatePosts title:string body:text"',
      'puts "rails generate migration AddAuthorToPosts author:string"',
      'puts "rails db:migrate"',
      'puts ""',
      'puts "=== Migration Class ==="',
      'class CreatePosts < Struct.new(:migrate)',
      '  def up',
      '    puts "  create_table :posts do |t|"',
      '    puts "    t.string :title"',
      '    puts "    t.text :body"',
      '    puts "    t.references :user, foreign_key: true"',
      '    puts "    t.timestamps  # created_at, updated_at"',
      '    puts "  end"',
      '  end',
      '  def down',
      '    puts "  drop_table :posts"',
      '  end',
      'end',
      'migration = CreatePosts.new(nil)',
      'migration.up',
      'migration.down',
      'puts ""',
      'puts "=== Schema (db/schema.rb) ==="',
      'puts "ActiveRecord::Schema.define(version: 1) do"',
      'puts "  create_table \'posts\', force: :cascade do |t|"',
      'puts "    t.string \'title\'"',
      'puts "    t.text \'body\'"',
      'puts "    t.datetime \'created_at\', null: false"',
      'puts "    t.datetime \'updated_at\', null: false"',
      'puts "  end"',
      'puts "end"',
      'puts ""',
      'puts "=== Seeds (db/seeds.rb) ==="',
      'posts = [',
      '  {title: "First Post", body: "Hello Rails", author: "Budi"},',
      '  {title: "Second Post", body: "MVC is great", author: "Siti"},',
      '  {title: "Third Post", body: "ActiveRecord", author: "Andi"},',
      ']',
      'posts.each_with_index do |p, i|',
      '  puts "Post.create(title: \'#{p[:title]}\', body: \'#{p[:body]}\')"',
      'end',
      'puts "rails db:seed"',
      'puts ""',
      'puts "=== Model with Validation ==="',
      'class Post',
      '  attr_accessor :title, :body, :author',
      '  def initialize(attrs = {})',
      '    @title = attrs[:title]',
      '    @body = attrs[:body]',
      '    @author = attrs[:author]',
      '  end',
      '  def valid?',
      '    @title && !@title.empty? && @body && !@body.empty?',
      '  end',
      'end',
      'p1 = Post.new({title: "Valid", body: "Content"})',
      'p2 = Post.new({title: "", body: "No title"})',
      'puts "Valid post: #{p1.valid?}"',
      'puts "Invalid post: #{p2.valid?}"',
      '',
    ].join('\n'),
    objectivesId: [
      'Migration: create_table, add_column, remove_column',
      'Rails generate migration: naming conventions',
      'Schema: db/schema.rb sebagai snapshot database',
      'Seeds: populasi data awal dengan db/seeds.rb',
      'Model validations: validates :title, presence: true',
    ],
    objectivesEn: [
      'Migrations: create_table, add_column, remove_column',
      'Rails generate migration: naming conventions',
      'Schema: db/schema.rb as database snapshot',
      'Seeds: populate initial data with db/seeds.rb',
      'Model validations: validates :title, presence: true',
    ],
    explanationId: '### Migration\nVersion control database. `create_table`, `add_column`, `rename_column`.\n\n### Generate\n`rails g migration CreatePosts title:string` - auto-generate timestamped migration.\n\n### Schema\n`db/schema.rb` - snapshot terkini. `rails db:schema:load` untuk setup baru.\n\n### Seeds\n`db/seeds.rb` - populate data. `rails db:seed`.\n\n### Validations\n`validates :title, presence: true, length: { minimum: 3 }`.\n\n### Commands\n`rails db:migrate`, `db:rollback`, `db:reset`',
    explanationEn: '### Migrations\nVersion control for databases. `create_table`, `add_column`.\n\n### Generate\n`rails g migration CreatePosts title:string` - auto-generates.\n\n### Schema\n`db/schema.rb` - current snapshot. `rails db:schema:load` for new setup.\n\n### Seeds\n`db/seeds.rb` - populate data. `rails db:seed`.\n\n### Validations\n`validates :title, presence: true, length: { minimum: 3 }`.\n\n### Commands\n`rails db:migrate`, `db:rollback`, `db:reset`.',
    experimentsId: [
      'Buat migration untuk posts, comments, users',
      'Tambah dan hapus column dengan migration',
      'Buat seeder dengan 10 data',
      'Implementasikan model validation',
      'Coba rollback migration',
    ],
    experimentsEn: [
      'Create migration for posts, comments, users',
      'Add and remove column with migration',
      'Create seeder with 10 records',
      'Implement model validation',
      'Try migration rollback',
    ],
    challengeId: 'Buat migration lengkap: users, posts, comments. Model dengan validasi. Seeder dengan data dummy.',
    challengeEn: 'Create complete migration: users, posts, comments. Models with validation. Seeder with dummy data.',
    summaryId: 'Minggu 3 dari 12: **Migrations & Database** (Level: Pemula). Data layer Rails. Minggu depan: **Views & ERB**.',
    summaryEn: 'Week 3 of 12: **Migrations & Database** (Level: Beginner). Data layer of Rails. Next week: **Views & ERB**.',
  },
  {
    week: 4, level: 'beginer', topicId: 'views-erb',
    titleId: 'Views & ERB Templates', titleEn: 'Views & ERB Templates',
    programId: 'Template Engine', programEn: 'Template Engine',
    levelNameId: 'Pemula', levelNameEn: 'Beginner',
    language: 'ruby',
    code: [
      '#!/usr/bin/env ruby',
      'puts "=== Rails Views (ERB) ==="',
      'puts ""',
      'puts "=== ERB Syntax ==="',
      'posts = [',
      '  {id: 1, title: "First Post", body: "Hello Rails"},',
      '  {id: 2, title: "Second Post", body: "MVC is great"},',
      '  {id: 3, title: "Third Post", body: "ERB templates"},',
      ']',
      'puts "=== Loop with each ==="',
      'puts "<% @posts.each do |post| %>"',
      'posts.each do |post|',
      '  puts "  <h2>#{post[:title]}</h2>"',
      '  puts "  <p>#{post[:body]}</p>"',
      'end',
      'puts "<% end %>"',
      'puts ""',
      'puts "=== If/Else ==="',
      'posts.each do |post|',
      '  status = post[:id] == 1 ? "active" : "inactive"',
      '  puts "  #{post[:title]}: #{status}"',
      'end',
      'puts ""',
      'puts "=== Link & Path Helpers ==="',
      'puts "<%= link_to \'Show\', post_path(post) %>"',
      'puts "<%= link_to \'Edit\', edit_post_path(post) %>"',
      'puts "<%= link_to \'Delete\', post_path(post), method: :delete %>"',
      'puts "<%= link_to \'Back\', posts_path %>"',
      'puts ""',
      'puts "=== Form Helpers ==="',
      'puts "<%= form_with model: @post do |f| %>"',
      'puts "  <%= f.label :title %>"',
      'puts "  <%= f.text_field :title %>"',
      'puts "  <%= f.label :body %>"',
      'puts "  <%= f.text_area :body %>"',
      'puts "  <%= f.submit \'Save\' %>"',
      'puts "<% end %>"',
      'puts ""',
      'puts "=== Layout & Partials ==="',
      'puts "<!-- app/views/layouts/application.html.erb -->"',
      'puts "<html>"',
      'puts "  <head><title><%= yield :title %></title></head>"',
      'puts "  <body>"',
      'puts "    <%= render \'shared/header\' %>"',
      'puts "    <%= yield %>"',
      'puts "    <%= render \'shared/footer\' %>"',
      'puts "  </body>"',
      'puts "</html>"',
      'puts ""',
      'puts "=== Render Partial ==="',
      'puts "<%= render @posts %>"',
      'puts "=> renders _post.html.erb for each post"',
      'puts ""',
      'puts "=== Instance Variables ==="',
      'puts "Controller: @posts = Post.all"',
      'puts "View: <% @posts.each do |post| %>"',
      '',
    ].join('\n'),
    objectivesId: [
      'ERB: <%= %> untuk output, <% %> untuk logic',
      'Partials: render reusable template components',
      'Layouts: application.html.erb sebagai master template',
      'Form helpers: form_with, text_field, submit',
      'Path helpers: post_path, posts_path, edit_post_path',
    ],
    objectivesEn: [
      'ERB: <%= %> for output, <% %> for logic',
      'Partials: render reusable template components',
      'Layouts: application.html.erb as master template',
      'Form helpers: form_with, text_field, submit',
      'Path helpers: post_path, posts_path, edit_post_path',
    ],
    explanationId: '### ERB Tags\n`<%= %>` output, `<% %>` logic only. `<%# %>` comment.\n\n### Partials\n`<%= render \'shared/header\' %>` - render partial file `_header.html.erb`.\n\n### Layout\n`application.html.erb` - master template. `yield` untuk content.\n\n### Form Helpers\n`form_with model: @post` - auto-detect create/edit. `f.text_field`, `f.submit`.\n\n### Path Helpers\n`posts_path` -> /posts, `post_path(post)` -> /posts/1, `edit_post_path(post)` -> /posts/1/edit.',
    explanationEn: '### ERB Tags\n`<%= %>` for output, `<% %>` for logic only.\n\n### Partials\n`<%= render \'shared/header\' %>` renders `_header.html.erb`.\n\n### Layouts\n`application.html.erb` - master template. `yield` for content.\n\n### Form Helpers\n`form_with model: @post` auto-detects create/edit. `f.text_field`, `f.submit`.\n\n### Path Helpers\n`posts_path` -> /posts, `post_path(post)` -> /posts/1, `edit_post_path(post)` -> /posts/1/edit.',
    experimentsId: [
      'Buat view dengan loop each dan conditional',
      'Buat partial untuk post card',
      'Implementasikan form_with untuk create/edit',
      'Buat custom layout untuk admin',
      'Coba content_for untuk custom section',
    ],
    experimentsEn: [
      'Create view with each loop and conditional',
      'Create partial for post card',
      'Implement form_with for create/edit',
      'Create custom layout for admin',
      'Try content_for for custom sections',
    ],
    challengeId: 'Buat view lengkap untuk blog: layout, partial header/footer, list posts dengan each, form create post.',
    challengeEn: 'Create complete views for blog: layout, partial header/footer, post list with each, form for creating posts.',
    summaryId: 'Minggu 4 dari 12: **Views & ERB Templates** (Level: Pemula). Selesai fase Beginner! Minggu depan: **Authentication** (Intermediate).',
    summaryEn: 'Week 4 of 12: **Views & ERB Templates** (Level: Beginner). Beginner phase complete! Next week: **Authentication** (Intermediate).',
  },
  // ── INTERMEDIATE (weeks 5-8) ──────────────────────────────────────────────
  {
    week: 5, level: 'intermediate', topicId: 'authentication',
    titleId: 'Authentication dengan Devise', titleEn: 'Authentication with Devise',
    programId: 'Login System', programEn: 'Login System',
    levelNameId: 'Menengah', levelNameEn: 'Intermediate',
    language: 'ruby',
    code: [
      '#!/usr/bin/env ruby',
      'puts "=== Rails Authentication (Devise) ==="',
      'puts ""',
      'puts "=== Install Devise ==="',
      'puts "gem \'devise\'"',
      'puts "bundle install"',
      'puts "rails generate devise:install"',
      'puts "rails generate devise User"',
      'puts "rails db:migrate"',
      'puts ""',
      'puts "=== User Model ==="',
      'class User',
      '  attr_accessor :email, :password, :role',
      '  def initialize(attrs = {})',
      '    @email = attrs[:email]',
      '    @password = attrs[:password]',
      '    @role = attrs[:role] || :user',
      '  end',
      '  def admin?',
      '    @role == :admin',
      '  end',
      'end',
      'users = [',
      '  User.new({email: "admin@mail.com", password: "secret123", role: :admin}),',
      '  User.new({email: "user@mail.com", password: "pass456", role: :user}),',
      ']',
      'puts "=== Login Simulation ==="',
      'input_email = "admin@mail.com"',
      'input_password = "secret123"',
      'user = users.find { |u| u.email == input_email }',
      'if user && user.password == input_password',
      '  puts "Login success! Welcome, #{user.email}"',
      '  puts "Admin? #{user.admin?}"',
      'else',
      '  puts "Login failed!"',
      'end',
      'puts ""',
      'puts "=== Devise Helpers ==="',
      'puts "user_signed_in?     - Is user logged in?"',
      'puts "current_user        - Current user object"',
      'puts "user_session        - Access session"',
      'puts ""',
      'puts "=== Routes ==="',
      'puts "devise_for :users"',
      'puts "new_user_session_path    - Login form"',
      'puts "destroy_user_session_path - Logout"',
      'puts "new_user_registration_path - Register"',
      'puts ""',
      'puts "=== Controller Auth ==="',
      'puts "class PostsController < ApplicationController"',
      'puts "  before_action :authenticate_user!"',
      'puts "  before_action :set_post, only: [:show, :edit, :update, :destroy]"',
      'puts "end"',
      'puts ""',
      'puts "=== Conditional Display ==="',
      'puts "<% if user_signed_in? %>"',
      'puts "  Welcome, <%= current_user.email %>"',
      'puts "  <%= link_to \'Logout\', destroy_user_session_path, method: :delete %>"',
      'puts "<% else %>"',
      'puts "  <%= link_to \'Login\', new_user_session_path %>"',
      'puts "<% end %>"',
      '',
    ].join('\n'),
    objectivesId: [
      'Devise gem: full authentication solution',
      'Install devise: generate, migrate',
      'Devise helpers: user_signed_in?, current_user',
      'Authenticate: before_action :authenticate_user!',
      'Registration, login, logout, password reset',
    ],
    objectivesEn: [
      'Devise gem: full authentication solution',
      'Install devise: generate, migrate',
      'Devise helpers: user_signed_in?, current_user',
      'Authenticate: before_action :authenticate_user!',
      'Registration, login, logout, password reset',
    ],
    explanationId: '### Devise\nFull auth solution: register, login, logout, password reset, email confirmation.\n\n### Install\n`gem \'devise\'`, `bundle install`, `rails g devise:install`, `rails g devise User`, `rails db:migrate`.\n\n### Helpers\n`user_signed_in?` - boolean, `current_user` - user object.\n\n### Protect Routes\n`before_action :authenticate_user!` - redirect ke login jika tidak auth.\n\n### Customization\nOverride views: `rails g devise:views`. Override controllers: `rails g devise:controllers users`.',
    explanationEn: '### Devise\nFull auth solution: register, login, logout, password reset, email confirmation.\n\n### Install\n`gem \'devise\'`, `bundle install`, `rails g devise:install`, `rails g devise User`, `rails db:migrate`.\n\n### Helpers\n`user_signed_in?` - boolean, `current_user` - user object.\n\n### Protect Routes\n`before_action :authenticate_user!` redirects to login if unauthenticated.\n\n### Customization\nOverride views: `rails g devise:views`. Override controllers: `rails g devise:controllers users`.',
    experimentsId: [
      'Install Devise dan setup User model',
      'Customize Devise views',
      'Add username field to Devise',
      'Implementasikan role-based access',
      'Coba omniauth untuk social login',
    ],
    experimentsEn: [
      'Install Devise and setup User model',
      'Customize Devise views',
      'Add username field to Devise',
      'Implement role-based access',
      'Try omniauth for social login',
    ],
    challengeId: 'Buat sistem auth lengkap: register, login, logout, role-based access (admin/user), protect routes.',
    challengeEn: 'Build a complete auth system: register, login, logout, role-based access (admin/user), protected routes.',
    summaryId: 'Minggu 5 dari 12: **Authentication dengan Devise** (Level: Menengah). Keamanan aplikasi. Minggu depan: **Associations**.',
    summaryEn: 'Week 5 of 12: **Authentication with Devise** (Level: Intermediate). Application security. Next week: **Associations**.',
  },
  {
    week: 6, level: 'intermediate', topicId: 'associations',
    titleId: 'Associations & Relationships', titleEn: 'Associations & Relationships',
    programId: 'Model Relations', programEn: 'Model Relations',
    levelNameId: 'Menengah', levelNameEn: 'Intermediate',
    language: 'ruby',
    code: [
      '#!/usr/bin/env ruby',
      'puts "=== Rails Associations ==="',
      'puts ""',
      'puts "=== One to Many (has_many / belongs_to) ==="',
      'class User',
      '  attr_accessor :name, :posts',
      '  def initialize(name)',
      '    @name = name',
      '    @posts = []',
      '  end',
      '  def add_post(post)',
      '    post.author = self',
      '    @posts << post',
      '  end',
      'end',
      'class Post',
      '  attr_accessor :title, :author, :comments',
      '  def initialize(title)',
      '    @title = title',
      '    @comments = []',
      '  end',
      'end',
      'budi = User.new("Budi")',
      'p1 = Post.new("First Post")',
      'p2 = Post.new("Second Post")',
      'budi.add_post(p1)',
      'budi.add_post(p2)',
      'puts "=== One to Many Simulation ==="',
      'puts "#{budi.name}\'s posts:"',
      'budi.posts.each { |p| puts "  - #{p.title}" }',
      'puts ""',
      'puts "=== Many to Many (has_many :through) ==="',
      'class Post2',
      '  attr_accessor :title, :tags',
      '  def initialize(title)',
      '    @title = title',
      '    @tags = []',
      '  end',
      'end',
      'class Tag',
      '  attr_accessor :name, :posts',
      '  def initialize(name)',
      '    @name = name',
      '    @posts = []',
      '  end',
      'end',
      'post = Post2.new("Rails Guide")',
      'tag1 = Tag.new("Ruby")',
      'tag2 = Tag.new("Rails")',
      'post.tags << tag1',
      'post.tags << tag2',
      'tag1.posts << post',
      'tag2.posts << post',
      'puts "#{post.title} tags:"',
      'post.tags.each { |t| puts "  - #{t.name}" }',
      'puts ""',
      'puts "=== Eager Loading ==="',
      'puts "Post.includes(:author, :comments).all"',
      'puts "Post.eager_load(:comments).where(comments: { approved: true })"',
      'puts "Post.preload(:author).limit(10)"',
      'puts ""',
      'puts "=== Nested Resources ==="',
      'puts "resources :posts do"',
      'puts "  resources :comments"',
      'puts "end"',
      'puts "# /posts/1/comments, /posts/1/comments/new"',
      'puts ""',
      'puts "=== Delegates ==="',
      'puts "class Comment < ApplicationRecord"',
      'puts "  belongs_to :post"',
      'puts "  delegate :author, to: :post"',
      'puts "end"',
      '',
    ].join('\n'),
    objectivesId: [
      'belongs_to: model memiliki foreign key',
      'has_many: model memiliki banyak record lain',
      'has_many :through: many-to-many dengan join model',
      'Eager loading: includes, eager_load, preload',
      'Nested resources: routes bersarang',
    ],
    objectivesEn: [
      'belongs_to: model has foreign key',
      'has_many: model has many other records',
      'has_many :through: many-to-many with join model',
      'Eager loading: includes, eager_load, preload',
      'Nested resources: nested routes',
    ],
    explanationId: '### belongs_to\nModel memiliki foreign key. `belongs_to :user` - punya `user_id`.\n\n### has_many\nModel memiliki banyak record. `has_many :posts` - user punya banyak post.\n\n### has_many :through\nMany-to-many via join table. `has_many :tags, through: :taggings`.\n\n### Eager Loading\n`includes(:comments)` - load relasi di 1-2 query. Tanpa ini = N+1 problem.\n\n### Nested Routes\n`resources :posts do resources :comments end` -> `/posts/1/comments`.',
    explanationEn: '### belongs_to\nModel has foreign key. `belongs_to :user` - has `user_id`.\n\n### has_many\nModel has many records. `has_many :posts`.\n\n### has_many :through\nMany-to-many via join table.\n\n### Eager Loading\n`includes(:comments)` loads relation in 1-2 queries. Without = N+1.\n\n### Nested Routes\n`resources :posts do resources :comments end` -> `/posts/1/comments`.',
    experimentsId: [
      'Buat relasi has_many/belongs_to',
      'Implementasikan has_many :through',
      'Coba eager loading vs lazy loading',
      'Buat nested resources routes',
      'Implementasikan polymorphic association',
    ],
    experimentsEn: [
      'Create has_many/belongs_to relation',
      'Implement has_many :through',
      'Try eager loading vs lazy loading',
      'Create nested resources routes',
      'Implement polymorphic association',
    ],
    challengeId: 'Buat blog dengan relasi: User has_many Posts, Post has_many Comments, Post has_many Tags through Taggings.',
    challengeEn: 'Create a blog with relations: User has_many Posts, Post has_many Comments, Post has_many Tags through Taggings.',
    summaryId: 'Minggu 6 dari 12: **Associations & Relationships** (Level: Menengah). Kekuatan ActiveRecord. Minggu depan: **Testing**.',
    summaryEn: 'Week 6 of 12: **Associations & Relationships** (Level: Intermediate). ActiveRecord\'s power. Next week: **Testing**.',
  },
  {
    week: 7, level: 'intermediate', topicId: 'testing',
    titleId: 'Testing dengan RSpec', titleEn: 'Testing with RSpec',
    programId: 'Test Suite', programEn: 'Test Suite',
    levelNameId: 'Menengah', levelNameEn: 'Intermediate',
    language: 'ruby',
    code: [
      '#!/usr/bin/env ruby',
      'puts "=== Rails Testing (RSpec) ==="',
      'puts ""',
      'puts "=== Setup ==="',
      'puts "group :development, :test do"',
      'puts "  gem \'rspec-rails\'"',
      'puts "  gem \'factory_bot_rails\'"',
      'puts "  gem \'faker\'"',
      'puts "end"',
      'puts "rails generate rspec:install"',
      'puts ""',
      'puts "=== Model Spec ==="',
      'class Post',
      '  attr_accessor :title, :body',
      '  def initialize(attrs = {})',
      '    @title = attrs[:title]',
      '    @body = attrs[:body]',
      '  end',
      '  def valid?',
      '    @title && !@title.empty? && @body && !@body.empty?',
      '  end',
      'end',
      'puts "RSpec Model Tests:"',
      'tests = [',
      '  {title: "Valid", body: "Content", expected: true},',
      '  {title: "No title", body: "", expected: false},',
      '  {title: "", body: "Content", expected: false},',
      '  {title: "A", body: "B", expected: true},',
      ']',
      'tests.each do |t|',
      '  p = Post.new({title: t[:title], body: t[:body]})',
      '  status = p.valid? == t[:expected] ? "PASS" : "FAIL"',
      '  puts "  #{status}: title=\'#{t[:title]}\' body=\'#{t[:body]}\' => #{p.valid?}"',
      'end',
      'puts ""',
      'puts "=== Request Spec ==="',
      'puts "RSpec.describe \'Posts\', type: :request do"',
      'puts "  describe \'GET /posts\' do"',
      'puts "    it \'returns all posts\' do"',
      'puts "      get posts_path"',
      'puts "      expect(response).to have_http_status(:ok)"',
      'puts "    end"',
      'puts "  end"',
      'puts "end"',
      'puts ""',
      'puts "=== Factory Bot ==="',
      'puts "FactoryBot.define do"',
      'puts "  factory :post do"',
      'puts "    title { Faker::Lorem.sentence }"',
      'puts "    body { Faker::Lorem.paragraph }"',
      'puts "    association :user"',
      'puts "  end"',
      'puts "end"',
      'puts ""',
      'puts "=== Test Helpers ==="',
      'puts "let(:user) { create(:user) }"',
      'puts "let(:post) { create(:post, user: user) }"',
      'puts "before { sign_in user }"',
      'puts ""',
      'puts "=== System Tests ==="',
      'puts "RSpec.describe \'Post management\', type: :system do"',
      'puts "  it \'creates a post\' do"',
      'puts "    visit new_post_path"',
      'puts "    fill_in \'Title\', with: \'Test Post\'"',
      'puts "    click_button \'Create\'"',
      'puts "    expect(page).to have_text(\'Test Post\')"',
      'puts "  end"',
      'puts "end"',
      '',
    ].join('\n'),
    objectivesId: [
      'RSpec: testing framework untuk Rails',
      'Model specs: test validasi dan business logic',
      'Request specs: test HTTP endpoints',
      'Factory Bot: test data generation',
      'System specs: browser-like integration tests',
    ],
    objectivesEn: [
      'RSpec: testing framework for Rails',
      'Model specs: test validations and business logic',
      'Request specs: test HTTP endpoints',
      'Factory Bot: test data generation',
      'System specs: browser-like integration tests',
    ],
    explanationId: '### RSpec Setup\n`gem \'rspec-rails\'`, `rails g rspec:install`.\n\n### Model Spec\nTest validasi, method, dan scope. `expect(post).to be_valid`.\n\n### Request Spec\nTest HTTP: `get posts_path`, `expect(response).to have_http_status(:ok)`.\n\n### Factory Bot\nDefine factories untuk test data. `create(:post)`, `build(:post)`.\n\n### System Spec\nCapybara-based: `visit`, `fill_in`, `click_button`, `expect(page).to have_text()`.\n\n### Helpers\n`let(:user) { create(:user) }`, `before { sign_in user }`.',
    explanationEn: '### RSpec Setup\n`gem \'rspec-rails\'`, `rails g rspec:install`.\n\n### Model Spec\nTest validations, methods, scopes. `expect(post).to be_valid`.\n\n### Request Spec\nTest HTTP: `get posts_path`, `expect(response).to have_http_status(:ok)`.\n\n### Factory Bot\nDefine factories for test data. `create(:post)`, `build(:post)`.\n\n### System Spec\nCapybara-based: `visit`, `fill_in`, `click_button`.\n\n### Helpers\n`let(:user) { create(:user) }`, `before { sign_in user }`.',
    experimentsId: [
      'Buat model spec dengan validasi',
      'Test controller dengan request spec',
      'Buat factory dengan Faker',
      'Implementasikan system test',
      'Coba test dengan mocking',
    ],
    experimentsEn: [
      'Create model spec with validations',
      'Test controller with request spec',
      'Create factory with Faker',
      'Implement system test',
      'Try test with mocking',
    ],
    challengeId: 'Buat test suite lengkap untuk Post: model spec (validations), request spec (CRUD), factory. Min 10 test cases.',
    challengeEn: 'Create a complete test suite for Post: model spec (validations), request spec (CRUD), factory. Min 10 test cases.',
    summaryId: 'Minggu 7 dari 12: **Testing dengan RSpec** (Level: Menengah). Kualitas kode terjamin. Minggu depan: **API Mode**.',
    summaryEn: 'Week 7 of 12: **Testing with RSpec** (Level: Intermediate). Code quality guaranteed. Next week: **API Mode**.',
  },
  {
    week: 8, level: 'intermediate', topicId: 'api-mode',
    titleId: 'Rails API Mode', titleEn: 'Rails API Mode',
    programId: 'REST API', programEn: 'REST API',
    levelNameId: 'Menengah', levelNameEn: 'Intermediate',
    language: 'ruby',
    code: [
      '#!/usr/bin/env ruby',
      'puts "=== Rails API Mode ==="',
      'puts ""',
      'puts "=== Create API Project ==="',
      'puts "rails new my_api --api"',
      'puts "# --api: skip views, skip assets, API-only middleware"',
      'puts ""',
      'puts "=== Routes ==="',
      'puts "Rails.application.routes.draw do"',
      'puts "  namespace :api do"',
      'puts "    namespace :v1 do"',
      'puts "      resources :posts, except: [:new, :edit]"',
      'puts "    end"',
      'puts "  end"',
      'puts "end"',
      'puts ""',
      'puts "=== API Controller ==="',
      'class Api::V1::PostsController',
      '  def index',
      '    posts = [',
      '      {id: 1, title: "First Post", body: "Hello"},',
      '      {id: 2, title: "Second Post", body: "API"},',
      '    ]',
      '    puts "GET /api/v1/posts"',
      '    posts.each { |p| puts "  #{p[:id]}: #{p[:title]}" }',
      '  end',
      '  def show',
      '    puts "GET /api/v1/posts/1"',
      '    puts "  {id: 1, title: \'First Post\', body: \'Hello\'}"',
      '  end',
      '  def create',
      '    puts "POST /api/v1/posts"',
      '    puts "  {id: 3, title: \'New Post\', body: \'Created\'}"',
      '  end',
      'end',
      'controller = Api::V1::PostsController.new',
      'controller.index',
      'controller.show',
      'controller.create',
      'puts ""',
      'puts "=== JSON Response ==="',
      'puts "render json: posts, status: :ok"',
      'puts "render json: { errors: post.errors }, status: :unprocessable_entity"',
      'puts "render json: { error: \'Not found\' }, status: :not_found"',
      'puts ""',
      'puts "=== Serializers (ActiveModel::Serializer) ==="',
      'puts "class PostSerializer < ActiveModel::Serializer"',
      'puts "  attributes :id, :title, :body, :created_at"',
      'puts "  belongs_to :user"',
      'puts "  has_many :comments"',
      'puts "end"',
      'puts ""',
      'puts "=== CORS ==="',
      'puts "# Gemfile: gem \'rack-cors\'"',
      'puts "config.middleware.insert_before 0, Rack::Cors do"',
      'puts "  allow do"',
      'puts "    origins \'*\'"',
      'puts "    resource \'*\', headers: :any, methods: [:get, :post, :put, :delete]"',
      'puts "  end"',
      'puts "end"',
      'puts ""',
      'puts "=== API Authentication ==="',
      'puts "# Token-based"',
      'puts "before_action :authenticate_user!"',
      'puts "token = request.headers[\'Authorization\']"',
      '',
    ].join('\n'),
    objectivesId: [
      'API mode: rails new --api untuk skip views',
      'Namespaced routes: api/v1/posts',
      'JSON responses: render json: dengan status codes',
      'Serializers: format JSON output',
      'CORS: cross-origin resource sharing',
    ],
    objectivesEn: [
      'API mode: rails new --api to skip views',
      'Namespaced routes: api/v1/posts',
      'JSON responses: render json: with status codes',
      'Serializers: format JSON output',
      'CORS: cross-origin resource sharing',
    ],
    explanationId: '### API Mode\n`rails new name --api` - skip views, assets, cookies. Lighter stack.\n\n### Namespaced Routes\n`namespace :api do namespace :v1 do resources :posts end end` -> `/api/v1/posts`.\n\n### JSON Response\n`render json: posts, status: :ok`. Status: :ok (200), :created (201), :not_found (404).\n\n### Serializers\n`ActiveModel::Serializer` atau `blueprinter` untuk format JSON.\n\n### CORS\n`rack-cors` gem untuk allow cross-origin requests.\n\n### Auth\nToken-based: `Authorization: Bearer <token>` header.',
    explanationEn: '### API Mode\n`rails new name --api` - skips views, assets, cookies.\n\n### Namespaced Routes\n`namespace :api do namespace :v1 do resources :posts end end`.\n\n### JSON Response\n`render json: posts, status: :ok`. Status: :ok (200), :created (201).\n\n### Serializers\n`ActiveModel::Serializer` or `blueprinter` for JSON formatting.\n\n### CORS\n`rack-cors` gem allows cross-origin requests.\n\n### Auth\nToken-based: `Authorization: Bearer <token>` header.',
    experimentsId: [
      'Buat API project dengan --api flag',
      'Implementasikan namespaced routes',
      'Buat serializer untuk Post model',
      'Coba API versioning',
      'Implementasikan JWT authentication',
    ],
    experimentsEn: [
      'Create API project with --api flag',
      'Implement namespaced routes',
      'Create serializer for Post model',
      'Try API versioning',
      'Implement JWT authentication',
    ],
    challengeId: 'Buat REST API lengkap untuk blog: CRUD posts, comments, auth dengan JWT, serializers, CORS.',
    challengeEn: 'Build a complete REST API for a blog: CRUD posts, comments, auth with JWT, serializers, CORS.',
    summaryId: 'Minggu 8 dari 12: **Rails API Mode** (Level: Menengah). Selesai fase Intermediate! Minggu depan: **Performance** (Advanced).',
    summaryEn: 'Week 8 of 12: **Rails API Mode** (Level: Intermediate). Intermediate phase complete! Next week: **Performance** (Advanced).',
  },
  // ── ADVANCED (weeks 9-12) ────────────────────────────────────────────────
  {
    week: 9, level: 'advanced', topicId: 'performance',
    titleId: 'Performance Optimization', titleEn: 'Performance Optimization',
    programId: 'Optimization', programEn: 'Optimization',
    levelNameId: 'Lanjutan', levelNameEn: 'Advanced',
    language: 'ruby',
    code: [
      '#!/usr/bin/env ruby',
      'puts "=== Rails Performance Optimization ==="',
      'puts ""',
      'puts "=== N+1 Query Problem ==="',
      'puts "# Bad: N+1 queries"',
      'puts "Post.all.each { |p| puts p.author.name }  # N+1 queries"',
      'puts ""',
      'puts "# Good: Eager loading"',
      'puts "Post.includes(:author).each { |p| puts p.author.name }  # 2 queries"',
      'puts ""',
      'puts "=== Bullet Gem ==="',
      'puts "gem \'bullet\', group: :development"',
      'puts "Bullet.enable = true"',
      'puts "Bullet.alert = true"',
      'puts "Bullet.add_footer = true"',
      'puts ""',
      'puts "=== Database Indexing ==="',
      'puts "add_index :posts, :user_id"',
      'puts "add_index :posts, :created_at"',
      'puts "add_index :posts, [:user_id, :created_at]"',
      'puts "add_index :users, :email, unique: true"',
      'puts ""',
      'puts "=== Caching ==="',
      'puts "# Fragment caching"',
      'puts "<% cache post do %>"',
      'puts "  <%= post.title %>"',
      'puts "<% end %>"',
      'puts ""',
      'puts "# Russian doll caching"',
      'puts "<% cache @posts do %>"',
      'puts "  <%= render @posts %>"',
      'puts "<% end %>"',
      'puts ""',
      'puts "# Low-level caching"',
      'puts "Rails.cache.fetch(\'recent_posts\', expires_in: 1.hour) do"',
      'puts "  Post.recent.to_a"',
      'puts "end"',
      'puts ""',
      'puts "=== Background Jobs ==="',
      'puts "class SendEmailJob < ApplicationJob"',
      'puts "  queue_as :default"',
      'puts "  def perform(user)"',
      'puts "    UserMailer.welcome(user).deliver_now"',
      'puts "  end"',
      'puts "end"',
      'puts "SendEmailJob.perform_later(user)"',
      'puts ""',
      'puts "=== Pagination ==="',
      'puts "# Gemfile: gem \'kaminari\'"',
      'puts "Post.page(1).per(10)"',
      'puts "Post.order(:created_at).page(params[:page])"',
      'puts ""',
      'puts "=== Database Optimization ==="',
      'puts "# EXPLAIN query"',
      'puts "Post.where(user_id: 1).explain"',
      'puts ""',
      'puts "# Counter cache"',
      'puts "belongs_to :user, counter_cache: true"',
      'puts "# users.posts_count column"',
      '',
    ].join('\n'),
    objectivesId: [
      'N+1 queries: detect dan solve dengan eager loading',
      'Bullet gem: auto-detect N+1 dan unused eager loading',
      'Database indexing: add_index untuk query cepat',
      'Caching: fragment, Russian doll, low-level caching',
      'Background jobs: perform_later untuk async processing',
    ],
    objectivesEn: [
      'N+1 queries: detect and solve with eager loading',
      'Bullet gem: auto-detect N+1 and unused eager loading',
      'Database indexing: add_index for fast queries',
      'Caching: fragment, Russian doll, low-level caching',
      'Background jobs: perform_later for async processing',
    ],
    explanationId: '### N+1 Problem\nLoop dengan relasi = N+1 query. Solve: `includes(:author)`.\n\n### Bullet\nAuto-detect N+1, unused eager loading, missing counter cache.\n\n### Indexing\n`add_index :posts, :user_id` - speed up WHERE queries. Unique index untuk uniqueness.\n\n### Caching\nFragment: cache partial. Russian doll: nested cache. Low-level: `Rails.cache.fetch`.\n\n### Background Jobs\n`perform_later` - enqueue job. Sidekiq/Resque untuk processing.\n\n### Pagination\n`kaminari` atau `pagy` - limit records per page.',
    explanationEn: '### N+1 Problem\nLoop with relations = N+1 queries. Solve: `includes(:author)`.\n\n### Bullet\nAuto-detects N+1, unused eager loading, missing counter cache.\n\n### Indexing\n`add_index :posts, :user_id` speeds up WHERE queries.\n\n### Caching\nFragment: cache partials. Russian doll: nested cache. Low-level: `Rails.cache.fetch`.\n\n### Background Jobs\n`perform_later` enqueues jobs. Sidekiq/Resque for processing.\n\n### Pagination\n`kaminari` or `pagy` limits records per page.',
    experimentsId: [
      'Detect N+1 dengan Bullet dan solve',
      'Add index dan benchmark query',
      'Implementasikan fragment caching',
      'Buat background job dengan Sidekiq',
      'Coba pagination dengan kaminari',
    ],
    experimentsEn: [
      'Detect N+1 with Bullet and solve',
      'Add index and benchmark query',
      'Implement fragment caching',
      'Create background job with Sidekiq',
      'Try pagination with kaminari',
    ],
    challengeId: 'Optimasi blog: detect N+1, add indexes, implement caching, add pagination. Benchmark before/after.',
    challengeEn: 'Optimize a blog: detect N+1, add indexes, implement caching, add pagination. Benchmark before/after.',
    summaryId: 'Minggu 9 dari 12: **Performance Optimization** (Level: Lanjutan). Speed matters. Minggu depan: **Background Jobs**.',
    summaryEn: 'Week 9 of 12: **Performance Optimization** (Level: Advanced). Speed matters. Next week: **Background Jobs**.',
  },
  {
    week: 10, level: 'advanced', topicId: 'background-jobs',
    titleId: 'Background Jobs & Sidekiq', titleEn: 'Background Jobs & Sidekiq',
    programId: 'Async Processing', programEn: 'Async Processing',
    levelNameId: 'Lanjutan', levelNameEn: 'Advanced',
    language: 'ruby',
    code: [
      '#!/usr/bin/env ruby',
      'puts "=== Rails Background Jobs ==="',
      'puts ""',
      'puts "=== Active Job ==="',
      'puts "class SendWelcomeEmailJob < ApplicationJob"',
      'puts "  queue_as :default"',
      'puts ""',
      'puts "  def perform(user)"',
      'puts "    UserMailer.welcome(user).deliver_now"',
      'puts "    Rails.logger.info \"Email sent to #{user.email}\""',
      'puts "  end"',
      'puts "end"',
      'puts ""',
      'puts "=== Dispatch Jobs ==="',
      'puts "SendWelcomeEmailJob.perform_later(user)    # Async"',
      'puts "SendWelcomeEmailJob.perform_now(user)      # Sync"',
      'puts "SendWelcomeEmailJob.set(wait: 5.minutes).perform_later(user)  # Delayed"',
      'puts "SendWelcomeEmailJob.set(queue: :high).perform_later(user)     # Priority"',
      'puts ""',
      'puts "=== Sidekiq ==="',
      'puts "gem \'sidekiq\'"',
      'puts "bundle exec sidekiq"',
      'puts "config.active_job.queue_adapter = :sidekiq"',
      'puts ""',
      'puts "=== Sidekiq Worker ==="',
      'puts "class ProcessImageWorker"',
      'puts "  include Sidekiq::Worker"',
      'puts ""',
      'puts "  def perform(image_id)"',
      'puts "    image = Image.find(image_id)"',
      'puts "    image.process!"',
      'puts "  end"',
      'puts "end"',
      'puts ""',
      'puts "=== Job Simulation ==="',
      'jobs = [',
      '  {id: 1, name: "SendWelcomeEmail", status: "completed", duration: "0.5s"},',
      '  {id: 2, name: "ProcessImage", status: "completed", duration: "2.3s"},',
      '  {id: 3, name: "GenerateReport", status: "processing", duration: "..."},',
      '  {id: 4, name: "CleanupOld", status: "queued", duration: "..."},',
      ']',
      'puts "ID | Job | Status | Duration"',
      'puts "---|-----|--------|----------"',
      'jobs.each do |j|',
      '  puts "#{j[:id]} | #{j[:name]} | #{j[:status]} | #{j[:duration]}"',
      'end',
      'puts ""',
      'puts "=== Job Chaining ==="',
      'puts "class OnboardingFlow"',
      'puts "  def self.start(user)"',
      'puts "    SendWelcomeEmailJob.perform_later(user)"',
      'puts "    SetupAccountJob.perform_later(user)"',
      'puts "    NotifyAdminJob.perform_later(user)"',
      'puts "  end"',
      'puts "end"',
      'puts ""',
      'puts "=== Error Handling ==="',
      'puts "class SendEmailJob < ApplicationJob"',
      'puts "  retry_on StandardError, wait: :exponentially_longer, attempts: 3"',
      'puts "  discard_on ActiveRecord::RecordNotFound"',
      'puts "end"',
      'puts ""',
      'puts "=== Monitoring ==="',
      'puts "Sidekiq Web UI: /sidekiq"',
      'puts "Sidekiq::Queue.new.size"',
      'puts "Sidekiq::RetrySet.new.size"',
      '',
    ].join('\n'),
    objectivesId: [
      'Active Job: framework-agnostic job interface',
      'Sidekiq: Redis-backed job processing',
      'Dispatch: perform_later, perform_now, set options',
      'Job chaining: multiple jobs in sequence',
      'Error handling: retry_on, discard_on',
    ],
    objectivesEn: [
      'Active Job: framework-agnostic job interface',
      'Sidekiq: Redis-backed job processing',
      'Dispatch: perform_later, perform_now, set options',
      'Job chaining: multiple jobs in sequence',
      'Error handling: retry_on, discard_on',
    ],
    explanationId: '### Active Job\nInterface untuk background jobs. Adapter: Sidekiq, Resque, Delayed Job.\n\n### Sidekiq\nRedis-based. Fast, efficient. `bundle exec sidekiq` untuk start worker.\n\n### Dispatch\n`perform_later` async, `perform_now` sync, `set(wait: 5.minutes)` delay.\n\n### Chaining\nMultiple jobs: `Job1.perform_later.then { Job2.perform_later }`.\n\n### Error Handling\n`retry_on` retry dengan backoff. `discard_on` skip job.\n\n### Monitoring\nSidekiq Web UI di `/sidekiq`. Monitor queues, retries, dead jobs.',
    explanationEn: '### Active Job\nInterface for background jobs. Adapters: Sidekiq, Resque, Delayed Job.\n\n### Sidekiq\nRedis-based. Fast, efficient. `bundle exec sidekiq` starts worker.\n\n### Dispatch\n`perform_later` async, `perform_now` sync, `set(wait:)` delay.\n\n### Chaining\nMultiple jobs in sequence.\n\n### Error Handling\n`retry_on` retries with backoff. `discard_on` skips job.\n\n### Monitoring\nSidekiq Web UI at `/sidekiq`. Monitor queues, retries, dead jobs.',
    experimentsId: [
      'Buat job dan dispatch ke Sidekiq',
      'Implementasikan job dengan retry_on',
      'Coba scheduled jobs dengan cron',
      'Buat batch jobs',
      'Monitor jobs dengan Sidekiq Web UI',
    ],
    experimentsEn: [
      'Create job and dispatch to Sidekiq',
      'Implement job with retry_on',
      'Try scheduled jobs with cron',
      'Create batch jobs',
      'Monitor jobs with Sidekiq Web UI',
    ],
    challengeId: 'Buat sistem email notification: queue email sending, retry 3x on failure, batch send, monitor dengan Sidekiq.',
    challengeEn: 'Build an email notification system: queue email sending, retry 3x on failure, batch send, monitor with Sidekiq.',
    summaryId: 'Minggu 10 dari 12: **Background Jobs & Sidekiq** (Level: Lanjutan). Async processing. Minggu depan: **Deployment**.',
    summaryEn: 'Week 10 of 12: **Background Jobs & Sidekiq** (Level: Advanced). Async processing. Next week: **Deployment**.',
  },
  {
    week: 11, level: 'advanced', topicId: 'deployment',
    titleId: 'Deployment & DevOps', titleEn: 'Deployment & DevOps',
    programId: 'Production Deploy', programEn: 'Production Deploy',
    levelNameId: 'Lanjutan', levelNameEn: 'Advanced',
    language: 'ruby',
    code: [
      '#!/usr/bin/env ruby',
      'puts "=== Rails Deployment ==="',
      'puts ""',
      'puts "=== Production Checklist ==="',
      'puts "1. RAILS_ENV=production"',
      'puts "2. SECRET_KEY_BASE set"',
      'puts "3. Database configured"',
      'puts "4. Asset precompile"',
      'puts "5. SSL/TLS enabled"',
      'puts ""',
      'puts "=== Environment Variables ==="',
      'puts "RAILS_ENV=production"',
      'puts "RAILS_MASTER_KEY=xxx"',
      'puts "DATABASE_URL=postgresql://user:pass@host/db"',
      'puts "REDIS_URL=redis://localhost:6379"',
      'puts "SECRET_KEY_BASE=xxx"',
      'puts ""',
      'puts "=== Asset Precompile ==="',
      'puts "rails assets:precompile"',
      'puts "RAILS_ENV=production rails assets:precompile"',
      'puts ""',
      'puts "=== Database ==="',
      'puts "rails db:migrate RAILS_ENV=production"',
      'puts "rails db:seed RAILS_ENV=production"',
      'puts ""',
      'puts "=== Deployment Platforms ==="',
      'platforms = [',
      '  "Heroku: git push heroku main",',
      '  "Render: auto-deploy from GitHub",',
      '  "Fly.io: fly launch",',
      '  "AWS: EC2, ECS, Elastic Beanstalk",',
      '  "DigitalOcean: App Platform",',
      ']',
      'platforms.each { |p| puts "  - #{p}" }',
      'puts ""',
      'puts "=== Docker ==="',
      'puts "FROM ruby:3.2"',
      'puts "RUN apt-get update && apt-get install -y nodejs postgresql-client"',
      'puts "WORKDIR /app"',
      'puts "COPY Gemfile* .⁄"',
      'puts "RUN bundle install"',
      'puts "COPY . ."',
      'puts "RUN rails assets:precompile"',
      'puts "CMD [\"rails\", \"server\", \"-b\", \"0.0.0.0\"]"',
      'puts ""',
      'puts "=== Capistrano ==="',
      'puts "gem \'capistrano\', group: :development"',
      'puts "cap production deploy"',
      'puts ""',
      'puts "=== Monitoring ==="',
      'puts "New Relic: APM"',
      'puts "Sentry: Error tracking"',
      'puts "Lograge: Structured logging"',
      'puts "Skylight: Rails performance"',
      'puts ""',
      'puts "=== Security ==="',
      'puts "force_ssl = true"',
      'puts "config.action_dispatch.default_headers = {"',
      'puts "  \'X-Frame-Options\' => \'DENY\',"',
      'puts "  \'X-Content-Type-Options\' => \'nosniff\',"',
      'puts "}"',
      '',
    ].join('\n'),
    objectivesId: [
      'Production checklist: env vars, database, assets',
      'Asset precompile: rails assets:precompile',
      'Deployment platforms: Heroku, Render, Fly.io, AWS',
      'Docker: containerize Rails app',
      'Monitoring: New Relic, Sentry, Lograge',
    ],
    objectivesEn: [
      'Production checklist: env vars, database, assets',
      'Asset precompile: rails assets:precompile',
      'Deployment platforms: Heroku, Render, Fly.io, AWS',
      'Docker: containerize Rails app',
      'Monitoring: New Relic, Sentry, Lograge',
    ],
    explanationId: '### Production Checklist\nSet env vars, configure DB, precompile assets, enable SSL.\n\n### Asset Precompile\n`rails assets:precompile` - compile CSS/JS untuk production.\n\n### Platforms\nHeroku: `git push heroku main`. Render: auto-deploy. Fly.io: `fly launch`.\n\n### Docker\nContainerize app. Multi-stage build untuk optimize image size.\n\n### Monitoring\nNew Relic: APM. Sentry: error tracking. Lograge: structured logs.\n\n### Security\n`force_ssl = true`. Security headers: X-Frame-Options, X-Content-Type-Options.',
    explanationEn: '### Production Checklist\nSet env vars, configure DB, precompile assets, enable SSL.\n\n### Asset Precompile\n`rails assets:precompile` compiles CSS/JS for production.\n\n### Platforms\nHeroku: `git push heroku main`. Render: auto-deploy. Fly.io: `fly launch`.\n\n### Docker\nContainerize app. Multi-stage builds optimize image size.\n\n### Monitoring\nNew Relic: APM. Sentry: error tracking. Lograge: structured logs.\n\n### Security\n`force_ssl = true`. Security headers: X-Frame-Options, X-Content-Type-Options.',
    experimentsId: [
      'Deploy ke Heroku atau Render',
      'Setup Docker untuk Rails app',
      'Implementasikan CI/CD pipeline',
      'Setup monitoring dengan Sentry',
      'Coba zero-downtime deployment',
    ],
    experimentsEn: [
      'Deploy to Heroku or Render',
      'Setup Docker for Rails app',
      'Implement CI/CD pipeline',
      'Setup monitoring with Sentry',
      'Try zero-downtime deployment',
    ],
    challengeId: 'Deploy Rails app ke production: setup env vars, precompile assets, configure database, enable SSL, setup monitoring.',
    challengeEn: 'Deploy Rails app to production: setup env vars, precompile assets, configure database, enable SSL, setup monitoring.',
    summaryId: 'Minggu 11 dari 12: **Deployment & DevOps** (Level: Lanjutan). Go live! Minggu depan: **Capstone Project**!',
    summaryEn: 'Week 11 of 12: **Deployment & DevOps** (Level: Advanced). Go live! Next week: **Capstone Project**!',
  },
  {
    week: 12, level: 'advanced', topicId: 'capstone-project',
    titleId: 'Capstone: E-Commerce Platform', titleEn: 'Capstone: E-Commerce Platform',
    programId: 'E-Commerce Rails', programEn: 'E-Commerce Rails',
    levelNameId: 'Lanjutan', levelNameEn: 'Advanced',
    language: 'ruby',
    code: [
      '#!/usr/bin/env ruby',
      'puts "=== Capstone: E-Commerce Platform ==="',
      'puts ""',
      'puts "=== Architecture ==="',
      'puts "Models: User, Product, Order, OrderItem, Category, Cart, Review"',
      'puts "Controllers: ProductsController, OrdersController, AuthController"',
      'puts "Services: OrderService, PaymentService, CartService"',
      'puts "Jobs: SendOrderConfirmationJob, ProcessPaymentJob"',
      'puts ""',
      'puts "=== Features ==="',
      'features = [',
      '  "User authentication (Devise)",',
      '  "Product catalog with search & filter",',
      '  "Shopping cart (session-based)",',
      '  "Order processing with state machine",',
      '  "Payment integration (Stripe)",',
      '  "Admin dashboard",',
      '  "Background jobs (Sidekiq)",',
      '  "API endpoints (JSON)",',
      '  "Testing (RSpec + Factory Bot)",',
      '  "Performance optimization",',
      '  "Docker deployment",',
      ']',
      'features.each { |f| puts "  ✓ #{f}" }',
      'puts ""',
      'puts "=== API Endpoints ==="',
      'endpoints = [',
      '  "POST /api/register" => "Register",',
      '  "POST /api/login" => "Login",',
      '  "GET /api/products" => "List products",',
      '  "GET /api/products/:id" => "Product detail",',
      '  "POST /api/orders" => "Create order",',
      '  "GET /api/orders" => "List orders",',
      '  "GET /api/orders/:id" => "Order detail",',
      '  "POST /api/admin/products" => "Create product (admin)",',
      ']',
      'endpoints.each { |ep, desc| puts "  #{ep} - #{desc}" }',
      'puts ""',
      'puts "=== Order Flow ==="',
      'puts "1. User browses products (GET /api/products)"',
      'puts "2. Add to cart (session-based)"',
      'puts "3. Checkout (POST /api/orders)"',
      'puts "4. Process payment (Stripe)"',
      'puts "5. Create order + order items"',
      'puts "6. Reduce product stock"',
      'puts "7. SendOrderConfirmationJob.perform_later(order)"',
      'puts "8. Return order with 201 status"',
      'puts ""',
      'puts "=== Testing Strategy ==="',
      'puts "Model specs: validations, associations, scopes"',
      'puts "Request specs: all API endpoints"',
      'puts "System specs: critical user flows"',
      'puts "Job specs: background job processing"',
      'puts ""',
      'puts "=== Deployment ==="',
      'puts "Docker + Docker Compose"',
      'puts "PostgreSQL + Redis"',
      'puts "Sidekiq for background jobs"',
      'puts "Nginx reverse proxy"',
      'puts "SSL with Let\'s Encrypt"',
      '',
    ].join('\n'),
    objectivesId: [
      'Menggabungkan semua konsep: MVC, auth, associations, testing, API',
      'E-commerce domain: products, orders, cart, payments',
      'Service objects: extract business logic dari controllers',
      'Background jobs: async processing untuk email dan payment',
      'Full-stack deployment: Docker, PostgreSQL, Redis, Sidekiq',
    ],
    objectivesEn: [
      'Combine all concepts: MVC, auth, associations, testing, API',
      'E-commerce domain: products, orders, cart, payments',
      'Service objects: extract business logic from controllers',
      'Background jobs: async processing for email and payment',
      'Full-stack deployment: Docker, PostgreSQL, Redis, Sidekiq',
    ],
    explanationId: '### Architecture\nMVC + Service Layer. Controller -> Service -> Model. Jobs untuk async.\n\n### E-Commerce Flow\nBrowse -> Add to cart -> Checkout -> Payment -> Order created -> Email sent.\n\n### Service Objects\nExtract logic: `OrderService.create(user, cart)`. Thin controllers.\n\n### Background Jobs\n`SendOrderConfirmationJob.perform_later(order)`. Sidekiq processing.\n\n### Testing\nModel specs, request specs, system specs, job specs.\n\n### Deployment\nDocker + Compose. PostgreSQL + Redis. Nginx + SSL.',
    explanationEn: '### Architecture\nMVC + Service Layer. Controller -> Service -> Model. Jobs for async.\n\n### E-Commerce Flow\nBrowse -> Add to cart -> Checkout -> Payment -> Order created -> Email sent.\n\n### Service Objects\nExtract logic: `OrderService.create(user, cart)`. Thin controllers.\n\n### Background Jobs\n`SendOrderConfirmationJob.perform_later(order)`. Sidekiq processing.\n\n### Testing\nModel specs, request specs, system specs, job specs.\n\n### Deployment\nDocker + Compose. PostgreSQL + Redis. Nginx + SSL.',
    experimentsId: [
      'Tambah payment webhook handler',
      'Implementasikan coupon/discount system',
      'Buat product review dan rating',
      'Tambah real-time notifications dengan ActionCable',
      'Buat admin dashboard dengn Blazer',
    ],
    experimentsEn: [
      'Add payment webhook handler',
      'Implement coupon/discount system',
      'Create product review and rating',
      'Add real-time notifications with ActionCable',
      'Create admin dashboard with Blazer',
    ],
    challengeId: 'Buat e-commerce platform lengkap: auth, products CRUD, cart, orders, payment, admin, API, testing, deployment.',
    challengeEn: 'Build a complete e-commerce platform: auth, products CRUD, cart, orders, payment, admin, API, testing, deployment.',
    summaryId: 'Minggu 12 dari 12: **Capstone: E-Commerce Platform** (Level: Lanjutan). Selesai! Anda sudah menguasai Ruby on Rails dari dasar hingga produksi.',
    summaryEn: 'Week 12 of 12: **Capstone: E-Commerce Platform** (Level: Advanced). Complete! You\'ve mastered Ruby on Rails from basics to production.',
  },
];

// Add weeks to levels
for (const level of LEVELS) {
  level.weeks = MODULES.filter(m => m.level === level.levelId).map(m => ({
    week: m.week,
    topicId: m.topicId,
    titleId: m.titleId,
    titleEn: m.titleEn,
  }));
}

gen.writeFiles(MODULES, LEVELS);
